spring:
  config:
    activate:
      on-profile: prod

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

logging:
  level:
    org.springframework.security: WARN
    root: INFO

management:
  endpoint:
    health:
      show-details: never
